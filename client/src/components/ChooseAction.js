import React from 'react';
import Button from 'react-bootstrap/Button';

const ChooseAction = () => {
  return (
    <div>
      <Button variant='primary'>Create Coupon</Button>
      <Button variant='primary'>Get Coupon</Button>
    </div>
  );
};

export default ChooseAction;
